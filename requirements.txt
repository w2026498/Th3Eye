# =============================================================================
# AI-Driven Real-Time Wireless Network Threat Detection & Source Localisation
# Requirements file for the FYP artefact
# =============================================================================

# Core wireless packet capture, injection & 802.11 analysis
scapy>=2.5.0

# Machine Learning (training & baseline models mentioned in project)
scikit-learn>=1.3.0

# Data processing & numerical operations (used with scikit-learn & feature extraction)
numpy>=1.24.0

# Data handling (pcap analysis, results logging)
pandas>=2.0.0

# Visualization (confusion matrices, RSSI heatmaps, performance charts for report)
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress bars (nice-to-have for long captures/training)
tqdm>=4.65.0
